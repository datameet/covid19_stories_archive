$(".intro-div-2 > p.cont-p.manual-into-cont-p").text(function(index, currentText) {
  if (currentText.length > 190) {
     return currentText.substr(0, 190) + '...'; }
  }
);

$("h4.art-heading-s.manual-single-article-heading").text(function(index, currentText) {
  if (currentText.length > 30) {
     return currentText.substr(0, 30) + '...'; }
  }
);

$("h4.art-heading-s.manual-art-heading-s").text(function(index, currentText) {
  if (currentText.length > 40) {
     return currentText.substr(0, 40) + '...'; }
  }
);


$(".most-p-right-cont .art-heading-s").text(function(index, currentText) {
  if (currentText.length > 80) {
     return currentText.substr(0, 80) + '...'; }
  }
);

$(".art-heading").text(function(index, currentText) {
  if (currentText.length > 80) {
     return currentText.substr(0, 80) + '...'; }
  }
);

$(".small-art-heading").text(function(index, currentText) {
  if (currentText.length > 60) {
     return currentText.substr(0, 60) + '...'; }
  }
);

$(".art-heading-s").text(function(index, currentText) {
  if (currentText.length > 50) {
     return currentText.substr(0, 50) + '...'; }
  }
);

$(".single-article-heading").text(function(index, currentText) {
  if (currentText.length > 50) {
     return currentText.substr(0, 50) + '...'; }
  }
);

$(".wid12-post-txt.arti-post-cont h2").text(function(index, currentText) {
  if (currentText.length > 40) {
     return currentText.substr(0, 40) + '...'; }
  }
);

// breaking news page
$("ul.news-status-ul li p").text(function(index, currentText) {
  if (currentText.length > 80) {
     return currentText.substr(0, 80) + '...'; }
  }
);

// enetertainment
$(".grey-card-article-within-gbg h4.art-heading.lra-with-gbg-art-heading").text(function(index, currentText) {
  if (currentText.length > 180) {
     return currentText.substr(0, 180) + '...'; }
  }
);
$("p.cont-p.review-cont-p, h4.art-heading-s.review-art-heading-s").text(function(index, currentText) {
  if (currentText.length > 20) {
     return currentText.substr(0, 20) + '...'; }
  }
);

$(".right-cont p.cont-p.review-cont-p, .right-cont h4.art-heading-s.review-art-heading-s").text(function(index, currentText) {
  if (currentText.length > 10) {
     return currentText.substr(0, 10) + '...'; }
  }
);

$(".brenews-h").text(function(index, currentText) {
  if (currentText.length > 100) {
     return currentText.substr(0, 100) + '...'; }
  }
);

$(".u-img span").text(function(index, currentText) {
  if (currentText.length > 18) {
     return currentText.substr(0, 18) + '...'; }
  }
);


$("p.today-p").text(function(index, currentText) {
  if (currentText.length > 12) {
     return currentText.substr(0, 12) + ''; }
  }
);
$(".title-p.today-article-title-p h4.today-art-h4").text(function(index, currentText) {
  if (currentText.length > 100) {
     return currentText.substr(0, 100) + ''; }
  }
);

$(".title-p.two-one-two-article-title-p h4.two-one-two-article-art-h").text(function(index, currentText) {
  if (currentText.length > 140) {
     return currentText.substr(0, 140) + '...'; }
  }
);

$(".intro-div > p.cont-p.manual-into-cont-p").text(function(index, currentText) {
  if (currentText.length > 140) {
     return currentText.substr(0, 140) + '#...'; }
  }
);


$("p.cont-p.prog-cont-p").text(function(index, currentText) {
  if (currentText.length > 140) {
     return currentText.substr(0, 140) + '...'; }
  }
);




$(".intro-div p").text(function(index, currentText) {
  if (currentText.length > 80) {
     return currentText.substr(0, 80) + '...'; }
  }
);


$(".three-articles.left-one-to-three-arti.dark-grey-bg .cont-p.intro-div").text(function(index, currentText) {
  if (currentText.length > 80) {
     return currentText.substr(0, 80) + '...'; }
  }
);

